apiVersion: v1
kind: ServiceAccount
metadata:
  name: xps13
  namespace: default
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: xps13-cluster-admin
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: cluster-admin
subjects:
- kind: ServiceAccount
  name: xps13
  namespace: default

# MASTER=nucle1.localdomain
# ssh $MASTER -- kubectl apply -f xps13.yaml
# SECRET=$(ssh $MASTER -- kubectl get sa xps13 -o jsonpath='{.secrets[0].name}')
# CA=$(ssh $MASTER -- kubectl get secret $SECRET -o jsonpath={'.data.ca\.crt'})
# TOKEN=$(ssh $MASTER -- kubectl get secret $SECRET -o jsonpath={'.data.token'} | base64 -d)

# kubectl config set-cluster nucles --server https://nucles.localdomain:6443 --certificate-authority <(echo $CA | python -m base64 -d) --embed-certs
# kubectl config set-credentials xps13-nucles --token $TOKEN
# kubectl config set-context nucles --cluster nucles --user xps13-nucles
